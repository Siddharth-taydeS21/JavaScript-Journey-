let score =    84 ;

let = result = score >= 35 ? "Pass" : "Fail";
console.log(result)

let login = false;

let visibility = login == true ? "is_logedin" : "please_log_in";
console.log(visibility)

let num = 45 ;
console.log(num * 3 == 74 ? "Correct" : "Incorrect")

let marks = 84 ;

let grade = marks >= 90 ? "A+" : marks >= 80 ? "A" : "B";
console.log(grade)